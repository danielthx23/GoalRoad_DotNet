This wwwroot folder is intentionally lightweight. Place static assets here (js, css, images).

Example structure:
- wwwroot/css/
- wwwroot/js/
- wwwroot/images/

If you need demo assets, I can add a basic CSS and JS starter.
